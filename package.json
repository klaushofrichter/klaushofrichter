{
  "name": "pages-browser",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "fetch-data": "node scripts/fetch-apps.mjs",
    "dev": "vite",
    "build": "npm run fetch-data && vue-tsc -b && vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "deploy": "git add -A && git commit -m 'deploy' && git push origin develop"
  },
  "dependencies": {
    "vue": "^3.5.25"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^24.10.1",
    "@vitejs/plugin-vue": "^6.0.2",
    "@vue/tsconfig": "^0.8.1",
    "typescript": "~5.9.3",
    "vite": "^7.3.1",
    "vue-tsc": "^3.1.5"
  }
}
