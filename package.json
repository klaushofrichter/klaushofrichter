{
  "name": "pages-browser",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "fetch-data": "node scripts/fetch-apps.mjs",
    "dev": "vite",
    "build": "npm run fetch-data && vue-tsc -b && vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "deploy": "npm run fetch-data && git add -A && git commit -m 'deploy' && git push origin develop"
  },
  "dependencies": {
    "vue": "^3.5.42"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.5.0",
    "@vitejs/plugin-vue": "^6.0.8",
    "@vue/tsconfig": "^0.9.1",
    "typescript": "~5.9.3",
    "vite": "^8.2.2",
    "vue-tsc": "^3.3.11"
  }
}
